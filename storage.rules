rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Les images sont stockées dans le dossier photos/{userId}/
    match /photos/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
